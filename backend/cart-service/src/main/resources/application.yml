server:
  port: 8082

spring:
  application:
    name: CART-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/cart_service_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 7290sql#pc2_GKJPW+janith@developer
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
